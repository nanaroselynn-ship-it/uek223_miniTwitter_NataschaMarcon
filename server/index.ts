// Import the main application instance from the local 'app' module
import app from './app'

// Export the instance as 'viteNodeApp' for integration with vite-plugin-node
export const viteNodeApp = app
