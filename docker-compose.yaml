# Docker Compose schema version
version: '3.8'

services:
  # Database service using MariaDB
  mariadb:
    image: mariadb:10.6.4-focal
    container_name: minitwitter-db
    # Use legacy authentication for compatibility with some drivers
    command: '--default-authentication-plugin=mysql_native_password'
    volumes:
      - m223_project_mariadb:/var/lib/mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=supersecret123
      - MYSQL_DATABASE=minitwitter
      - MYSQL_USER=minitwitter
      - MYSQL_PASSWORD=supersecret123
    ports:
      - "3306:3306"

  # Database management tool (Web UI)
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: minitwitter-phpmyadmin
    depends_on:
      - mariadb
    restart: always
    environment:
      - PMA_HOST=mariadb
      - PMA_PORT=3306
      - MYSQL_ROOT_PASSWORD=supersecret123
    ports:
      - "9200:80"

  # The Node.js application service
  app:
    build: .
    container_name: minitwitter-app
    depends_on:
      - mariadb
    ports:
      - "4200:4200"
    environment:
      - DB_HOST=mariadb
      - DB_USER=minitwitter
      - DB_PASSWORD=supersecret123
      - DB_NAME=minitwitter
      - NODE_ENV=development
    volumes:
      - .:/app
      # Ensure node_modules are kept within the container
      - /app/node_modules
    command: npm run dev

# Named volume for persistent database storage
volumes:
  m223_project_mariadb:
